

class StringCalculator {
  constructor(){}

  // String calculator function, takes in a string with comma-separated numbers and returns the sum
  calculateValues(numbers: string): number {
    return 0;
  }

}
